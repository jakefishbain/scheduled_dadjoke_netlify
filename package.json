{
  "name": "scheduled_dadjoke_vercel",
  "version": "1.0.0",
  "description": "Vercel cron job to send dad joke emails",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "check": "node --check api/dadjoke-sender.ts"
  },
  "dependencies": {
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "vercel": "^48.1.6"
  }
}
